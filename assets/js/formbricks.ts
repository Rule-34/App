import formbricks from '@formbricks/js'

if (typeof window !== 'undefined') {
  formbricks.setup({
    environmentId: 'cm6gbkjeq0008l7036ckdvtnm',
    appUrl: 'https://app.formbricks.com'
  })
}

export default formbricks
