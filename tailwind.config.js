module.exports = {
  theme: {
    extend: {

      inset: {
        'minus-1/4': '-25%',
        'minus-3/4': '-75%',
        'minus-100': '-100%',
      }

      // linearGradients: theme => ({
      //   colors: theme('colors'),
      // }),
      // radialGradients: theme => ({
      //   colors: theme('colors'),
      // }),
      // conicGradients: theme => ({
      //   colors: theme('colors'),
      // }),

    }
  },
  variants: {},
  // plugins: [require('tailwindcss-gradients')()]
};